<Application x:Class="LightAddonApp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:LightAddonApp"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="App.BackgroundBrush" Color="#111111"/>
        <SolidColorBrush x:Key="App.SurfaceBrush" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="App.BorderBrush" Color="#333333"/>
        <SolidColorBrush x:Key="App.PrimaryBrush" Color="#4CAF50"/>
        <SolidColorBrush x:Key="App.PrimaryBrushMuted" Color="#335C43"/>
        <SolidColorBrush x:Key="App.TextBrush" Color="#F5F5F5"/>
        <SolidColorBrush x:Key="App.TextSecondaryBrush" Color="#AAAAAA"/>

        <Style TargetType="Window">
            <Setter Property="Background" Value="{StaticResource App.BackgroundBrush}" />
            <Setter Property="Foreground" Value="{StaticResource App.TextBrush}" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="13" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource App.PrimaryBrushMuted}" />
            <Setter Property="Foreground" Value="{StaticResource App.TextBrush}" />
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="4,0"/>
            <Setter Property="BorderBrush" Value="{StaticResource App.BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource App.PrimaryBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ToolbarLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource App.TextSecondaryBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
        </Style>
    </Application.Resources>
</Application>
